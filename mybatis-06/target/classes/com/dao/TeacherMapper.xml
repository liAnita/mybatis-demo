<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace = "需要指定的mapper接口"-->
<mapper namespace="com.dao.TeacherMapper">
    <!-- 方式一：按照结果嵌套查询-->
    <select id="selectTeacher" resultMap="teacherStudent1">
        select s.id sid,s.name sname, t.id tid,t.name tname from student s,teacher t where s.tid = t.id and t.id = #{tid}
    </select>
    <resultMap id="teacherStudent1" type="Teacher">
        <result property="id" column="tid"></result>
        <result property="name" column="tname"></result>
        <collection property="students" ofType="Student">
            <result property="id" column="sid"></result>
            <result property="name" column="sname"></result>
        </collection>
    </resultMap>
    <!--  方式二：查询嵌套-->
    <select id="selectTeacher2" resultMap="teacherStudent2">
        select *  from teacher where id = #{tid}
    </select>
    <resultMap id="teacherStudent2" type="Teacher">
        <collection property="students" ofType="Student" select="getStudentByTid" column="id">
            <result property="id" column="id"></result>
            <result property="name" column="name"></result>
        </collection>
    </resultMap>
    <select id="getStudentByTid" resultType="Student">
        select * from student where tid = #{tid}
    </select>
</mapper>

