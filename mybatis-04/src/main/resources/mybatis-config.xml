<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!--configuration 核心配置文件-->
<configuration>
    <properties resource="db.properties"></properties>
    <settings>
        <setting name="logImpl" value="LOG4J"/>
    </settings>
    <typeAliases>
<!--        第一种方式，给实体类指定一个别名，配置文件中不需要使用全路径名称-->
<!--        <typeAlias type="com.pojo.User" alias="user"/>-->
        <!--第二种方式：指定某个包路径，
        1、MyBatis会在包名下面搜索需要的 Java Bean
        2、在没有注解的情况下，会使用 Bean 的首字母小写的非限定类名来作为它的别名
        3、若有注解，则别名为其注解值-->
        <package name="com.pojo"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
<!--    三种映射器-->
<!--方式一：使用相对于类路径的资源引用 【推荐】-->
<!--        <mapper resource="com/dao/UserMapper.xml"></mapper>-->
        <!-- 方式二：使用映射器接口实现类的完全限定类名
          注意点：
        1、接口和他的Mapper配置文件必须同名
        1、接口和他的Mapper配置文件必须在同一个包下！-->
        <mapper class="com.dao.UserMapper"></mapper>
<!--     方式三: 将包内的映射器接口实现全部注册为映射器，注意点同方式二一致   -->
<!--        <package name="com.dao"/>-->
    </mappers>
</configuration>
